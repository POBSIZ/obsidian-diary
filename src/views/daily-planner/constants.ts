/** Timed entries at or below this duration omit their time label. */
export const COMPACT_EVENT_THRESHOLD_MINUTES = 30;

/** Timed entries below this duration use the extra-compact visual treatment. */
export const SHORT_EVENT_THRESHOLD_MINUTES = 20;

/** Minimum rendered and collision-aware height for a timed entry, in pixels/minutes. */
export const MIN_VISUAL_EVENT_DURATION_MINUTES = 24;
